import 'package:flutter/material.dart';

class BookNowPageMobile extends StatelessWidget {
  const BookNowPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qui puoi prenotare una sala'),
    );
  }
}
