import 'package:flutter/material.dart';

class JamPage extends StatelessWidget {
  const JamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Pagina Jam')));
  }
}
