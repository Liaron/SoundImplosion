import 'package:flutter/material.dart';

class FindJamPageMobile extends StatelessWidget {
  const FindJamPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qui puoi cercare una Jam Session a cui unirti'),
    );
  }
}
