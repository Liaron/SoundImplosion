import 'package:flutter/material.dart';

class OrganizeJamPageMobile extends StatelessWidget {
  const OrganizeJamPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Qui puoi organizzare una Jam Session'),
    );
  }
}
