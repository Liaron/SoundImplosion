import 'package:flutter/material.dart';

class MyBookingsPageMobile extends StatelessWidget {
  const MyBookingsPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina Le Mie Prenotazioni'),
    );
  }
}
